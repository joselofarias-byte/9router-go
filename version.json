{
  "latestVersion": "1.8.9",
  "downloadUrl": "",
  "releaseNotes": "Fork build: upstream 1.8.9 plus the validated minimal Fabric control plane. Automatic binary replacement is intentionally fail-closed until this fork publishes its own release assets. Use the CI-validated Termux artifact or explicitly configure UPDATE_URL/UPDATE_REPO for a trusted update source."
}
